import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'it.kamianets.com',
	appName: 'IT Kamianets',
	webDir: 'dist/theme-tailwind/browser',
};

export default config;
